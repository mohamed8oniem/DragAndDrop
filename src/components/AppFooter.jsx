const AppFooter = () => {
  return (
    <footer className="mt-auto py-6 text-center">
      <p className="text-gray-600">© {new Date().getFullYear()} Drag & Drop</p>
    </footer>
  );
};

export default AppFooter;
